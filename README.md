# Проект 7-го спринта

### Описание
Репозиторий предназначен для сдачи проекта 7-го спринта

### Как работать с репозиторием
1. В вашем GitHub-аккаунте автоматически создастся репозиторий `de-project-sprint-7` после того, как вы привяжете свой GitHub-аккаунт на Платформе.
2. Скопируйте репозиторий на свой компьютер. В качестве пароля укажите ваш `Access Token`, который нужно получить на странице [Personal Access Tokens](https://github.com/settings/tokens)):
	* `git clone https://github.com/{{ username }}/de-project-sprint-7.git`
3. Перейдите в директорию с проектом: 
	* `cd de-project-sprint-7`
4. Выполните проект и сохраните получившийся код в локальном репозитории:
	* `git add .`
	* `git commit -m 'my best commit'`
5. Обновите репозиторий в вашем GutHub-аккаунте:
	* `git push origin main`

### Структура репозитория
Вложенные файлы в репозиторий будут использоваться для проверки и предоставления обратной связи по проекту. Поэтому постарайтесь публиковать ваше решение согласно установленной структуре — так будет проще соотнести задания с решениями.

Внутри `src` расположены две папки:
- `/src/dags`;
- `/src/sql`.

/analytics/
├── user_geo/                     # основная витрина (итоговый продукт)
│   ├── dt=2025-02-17/            # партиционирование по дате расчёта (yyyy-MM-dd)
│   │   ├── _SUCCESS
│   │   ├── part-00000-....parquet
│   │   └── ...
│   ├── _SUCCESS                  # (иногда оставляют в корне витрины)
│   └── latest/                   # символическая ссылка или view на самую свежую партицию
│
├── source/                       # сырые / почти сырые данные
│   ├── events/                   # сырые события пользователей (если используются)
│   │   └── dt=2025-02-16/
│   └── geo/                      # справочник городов (обновляется редко)
│       └── geo.csv               # или geo.parquet (если уже конвертировали)
│
├── intermediate/                 # промежуточные таблицы (обычно временные)
│   ├── user_city_candidates/     # после cross join / до вычисления расстояний
│   └── user_act_city_raw/        # промежуточный результат до очистки / агрегации
│
└── analytics_team/               # удобные витрины / отчёты специально для аналитиков
├── user_geo_flat_daily/      # денормализованная версия без партиций по времени
└── user_geo_report/          # агрегированные отчёты (по городам, регионам и т.д.)
text## Основные директории и их назначение

| Путь                              | Назначение                                                                 | Формат     | Обновление              | Доступ                  |
|-----------------------------------|----------------------------------------------------------------------------|------------|--------------------------|--------------------------|
| `/analytics/user_geo/dt=YYYY-MM-DD/` | Основная витрина проекта — актуальный город пользователя на дату расчёта | Parquet    | ежедневно (ночью)       | все аналитики + BI      |
| `/analytics/source/geo/`          | Справочник городов (lat, lon, city name)                                   | CSV → Parquet | раз в 3–6 месяцев или по необходимости | только разработчики / ETL |
| `/analytics/intermediate/`        | Временные / отладочные данные, не предназначены для длительного хранения   | Parquet    | каждый запуск (перезаписываются) | только разработчики |
| `/analytics/analytics_team/`      | Удобные представления для аналитиков (flat-таблицы, агрегации)             | Parquet    | ежедневно / еженедельно | все аналитики + BI      |

## Партиционирование и обновление данных

- **Основная витрина** `/analytics/user_geo/`  
  Партиционируется по дате расчёта: `dt=YYYY-MM-DD`  
  Обновление: **ежедневно** (обычно в 3:00–5:00 утра)  
  Retention: храним последние **30–90 дней** (настраивается в зависимости от объёма и требований)

- **Справочник geo**  
  Обновляется **очень редко** (при изменении списка городов, добавлении новых или исправлении координат)  
  Рекомендуется держать в формате Parquet с одним файлом (репартиционировать при обновлении)

- **Промежуточные таблицы**  
  Перезаписываются при каждом запуске ETL (режим `overwrite`)  
  Рекомендуется чистить старше 7–14 дней (через airflow или cron)

## Формат хранения

- **Основной формат** — **Parquet** (сжатие snappy или zstd)  
  - columnar → быстрое чтение по колонкам  
  - predicate pushdown → фильтрация на уровне файлов  
  - хорошее сжатие → экономия места

- Справочник городов может храниться как **CSV** (для простоты редактирования человеком) или уже в **Parquet**

- *Не используем*:
  - CSV/JSON в больших объёмах
  - ORC (если нет очень специфичных требований Hive/Presto)
  - текст без сжатия

## Рекомендации аналитикам

Для большинства задач удобнее всего использовать таблицу:

```sql
SELECT *
FROM analytics.user_geo
WHERE dt = current_date - interval '1' day